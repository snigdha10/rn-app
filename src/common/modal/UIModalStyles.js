import { StyleSheet } from "react-native";

export default StyleSheet.create({
    modalContainer: {
        justifyContent: 'center',
        alignItems: 'center',
        flex: 1
    },
    modalClose: {
        color: 'red',
        paddingVertical: 20
    },
    modalChildren: {
        width: '80%',
        marginBottom: 20
    }
});
